# Copy this file to  keys.env  and fill in your MEXC keys.
# keys.env is gitignored, so your keys are never committed.
# These are loaded automatically by run_daily.sh for scheduled (cron) runs.
export MEXC_API_KEY="your_key_here"
export MEXC_API_SECRET="your_secret_here"
